---
name: Bug report
about: Something behaved wrong
labels: bug
---

**The command** (verbatim, with `--max-calls` if it spends):

```console
$ smartpipe …
```

**What happened** (stdout/stderr, trimmed):

**What you expected:**

**Environment:** `smartpipe --version`, OS, provider/model (`smartpipe config show` — no secrets in it), and `smartpipe doctor` output if setup-related.
